"""Pydantic Schema 模块。

定义前后端 API 交互的数据结构。与 ORM 模型不同，
Schema 专注于网络传输层的类型校验和序列化。

Modules:
    game   — 游戏运行时 Schema（Frame, GameState, ChoiceResult 等）
    editor — 可视化编辑器 Schema（NodeCreate, ChoiceCreate 等）
"""
