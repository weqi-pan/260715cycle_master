# Cycle Master — 后端开发依赖
# 安装方式: pip install -r backend/requirements-dev.txt

# 测试
pytest>=9.1
httpx>=0.28         # FastAPI TestClient 依赖

# 开发服务器
uvicorn>=0.34
watchfiles>=1.2     # uvicorn --reload 依赖

# 代码质量（可选）
# ruff>=0.8         # Python linter
# mypy>=1.13        # 类型检查
