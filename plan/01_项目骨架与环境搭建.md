# Phase 0: 项目骨架与环境搭建

> **工期**：1 周（第 1 周）  
> **目标**：前后端项目骨架就绪，本地可启动运行  
> **前置**：无  
> **产出**：可运行的空项目、开发规范文档

---

## 一、任务清单

### 1.1 后端项目初始化（Day 1-2）

#### 1.1.1 项目结构创建

```
backend/
├── app/
│   ├── __init__.py
│   ├── main.py                  # FastAPI 入口
│   ├── config.py                # 配置管理（环境变量 + .env）
│   ├── database.py              # SQLAlchemy 引擎 + Session 工厂
│   ├── models/
│   │   ├── __init__.py
│   │   ├── story.py             # StoryNode, Choice（暂空）
│   │   └── save.py              # Save, NodePersistentState（暂空）
│   ├── schemas/
│   │   ├── __init__.py
│   │   ├── game.py              # GameState, Frame, ChoiceResult（暂空）
│   │   └── editor.py            # NodeCreate, ChoiceCreate（暂空）
│   ├── engine/
│   │   ├── __init__.py
│   │   ├── graph.py             # 图结构加载（Phase 2）
│   │   ├── engine.py            # 游戏主循环（Phase 2）
│   │   ├── condition_eval.py    # 条件解析器（Phase 2）
│   │   └── special_router.py    # 特殊路由（Phase 2）
│   └── routers/
│       ├── __init__.py
│       ├── game.py              # /api/game/*（Phase 3）
│       ├── saves.py             # /api/saves/*（Phase 3）
│       └── editor.py            # /api/editor/*（Phase 5）
├── tests/
│   ├── __init__.py
│   ├── conftest.py
│   ├── test_engine/             # Phase 2
│   └── test_api/                # Phase 3
├── requirements.txt
├── requirements-dev.txt
└── .env.example
```

#### 1.1.2 依赖安装

**requirements.txt**：
```
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
sqlalchemy>=2.0.25
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0
aiosqlite>=0.19.0
```

**requirements-dev.txt**：
```
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.26.0          # TestClient 依赖
ruff>=0.2.0
pyright>=1.1.350
```

#### 1.1.3 入口文件（main.py）

```python
from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware
from app.config import settings

app = FastAPI(
    title="Cycle Master API",
    version="0.1.0",
    description="莫比乌斯环无限循环视觉小说游戏后端",
)

app.add_middleware(
    CORSMiddleware,
    allow_origins=settings.CORS_ORIGINS,
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)

@app.get("/api/health")
async def health_check():
    return {"status": "ok", "version": "0.1.0"}

# Router 注册（后续 Phase 逐步添加）
# from app.routers import game, saves, editor
# app.include_router(game.router, prefix="/api/game", tags=["game"])
# app.include_router(saves.router, prefix="/api/saves", tags=["saves"])
# app.include_router(editor.router, prefix="/api/editor", tags=["editor"])
```

#### 1.1.4 配置管理（config.py）

```python
from pydantic_settings import BaseSettings

class Settings(BaseSettings):
    # 应用
    APP_NAME: str = "Cycle Master"
    APP_VERSION: str = "0.1.0"
    DEBUG: bool = True

    # 数据库
    DATABASE_URL: str = "sqlite+aiosqlite:///./cycle_master.db"

    # CORS
    CORS_ORIGINS: list[str] = [
        "http://localhost:5173",  # Vite dev server
        "http://localhost:1420",  # Tauri dev server
    ]

    class Config:
        env_file = ".env"
        env_file_encoding = "utf-8"

settings = Settings()
```

#### 1.1.5 数据库引擎（database.py）

```python
from sqlalchemy.ext.asyncio import create_async_engine, async_sessionmaker, AsyncSession
from sqlalchemy.orm import DeclarativeBase
from app.config import settings

engine = create_async_engine(settings.DATABASE_URL, echo=settings.DEBUG)
async_session = async_sessionmaker(engine, class_=AsyncSession, expire_on_commit=False)

class Base(DeclarativeBase):
    pass

async def get_db() -> AsyncSession:
    async with async_session() as session:
        yield session

async def init_db():
    """创建所有表（开发阶段使用，生产应使用 Alembic）"""
    async with engine.begin() as conn:
        await conn.run_sync(Base.metadata.create_all)
```

---

### 1.2 前端项目初始化（Day 3-4）

#### 1.2.1 项目创建

```bash
npm create vite@latest frontend -- --template vue-ts
cd frontend
npm install
```

#### 1.2.2 依赖安装

```bash
# 核心依赖
npm install vue-router@4 pinia@2 element-plus @element-plus/icons-vue
npm install axios

# 开发依赖
npm install -D sass @types/node
# Cytoscape.js 后补（Phase 5）
```

#### 1.2.3 项目结构

```
frontend/
├── src/
│   ├── main.ts                  # 入口
│   ├── App.vue                  # 根组件
│   ├── router/
│   │   └── index.ts             # 路由配置
│   ├── stores/
│   │   ├── gameStore.ts         # 游戏运行时状态（Phase 4）
│   │   └── editorStore.ts       # 编辑器状态（Phase 5）
│   ├── api/
│   │   ├── client.ts            # Axios 实例 + baseURL
│   │   ├── game.ts              # /api/game/*（Phase 4）
│   │   ├── saves.ts             # /api/saves/*（Phase 4）
│   │   └── editor.ts            # /api/editor/*（Phase 5）
│   ├── types/
│   │   └── index.ts             # TS 类型定义（Phase 1 开始）
│   ├── views/
│   │   ├── HomePage.vue         # 首页（Phase 4）
│   │   ├── GamePlay.vue         # 游戏播放器（Phase 4）
│   │   └── EditorLayout.vue     # 编辑器（Phase 5）
│   ├── components/
│   │   ├── player/              # 播放器组件（Phase 4）
│   │   └── editor/              # 编辑器组件（Phase 5）
│   └── assets/
│       └── styles/
│           ├── variables.scss   # SCSS 变量
│           ├── reset.scss       # 样式重置
│           └── global.scss      # 全局样式
├── index.html
├── vite.config.ts
├── tsconfig.json
└── package.json
```

#### 1.2.4 Vite 配置

```typescript
// vite.config.ts
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
import { resolve } from 'path'

export default defineConfig({
  plugins: [vue()],
  resolve: {
    alias: {
      '@': resolve(__dirname, 'src'),
    },
  },
  server: {
    port: 5173,
    proxy: {
      '/api': {
        target: 'http://localhost:8000',
        changeOrigin: true,
      },
    },
  },
  css: {
    preprocessorOptions: {
      scss: {
        additionalData: `@use "@/assets/styles/variables.scss" as *;`,
      },
    },
  },
})
```

#### 1.2.5 路由配置

```typescript
// src/router/index.ts
import { createRouter, createWebHistory } from 'vue-router'

const router = createRouter({
  history: createWebHistory(),
  routes: [
    {
      path: '/',
      name: 'home',
      component: () => import('@/views/HomePage.vue'),
    },
    {
      path: '/play',
      name: 'play',
      component: () => import('@/views/GamePlay.vue'),
    },
    {
      path: '/editor',
      name: 'editor',
      component: () => import('@/views/EditorLayout.vue'),
    },
  ],
})

export default router
```

#### 1.2.6 API 客户端

```typescript
// src/api/client.ts
import axios from 'axios'

const apiClient = axios.create({
  baseURL: '/api',
  timeout: 10000,
  headers: { 'Content-Type': 'application/json' },
})

// 响应拦截器
apiClient.interceptors.response.use(
  (response) => response.data,
  (error) => {
    console.error('API Error:', error.response?.data || error.message)
    return Promise.reject(error)
  }
)

export default apiClient
```

---

### 1.3 开发规范建立（Day 4-5）

#### 1.3.1 代码风格

- **Python**：Ruff 格式化 + lint，pyright 类型检查
- **TypeScript**：ESLint + Prettier，Vue 官方 ESLint 配置
- **Git 提交**：Conventional Commits（`feat:`, `fix:`, `docs:`, `refactor:`, `test:` 等）

#### 1.3.2 命名约定

| 语言 | 变量/函数 | 类/组件 | 文件 |
|------|----------|---------|------|
| Python | `snake_case` | `PascalCase` | `snake_case.py` |
| TypeScript | `camelCase` | `PascalCase` | `PascalCase.vue` / `camelCase.ts` |
| SCSS | `kebab-case` | - | `kebab-case.scss` |
| SQL 表 | `snake_case` | - | - |

#### 1.3.3 .editorconfig

```ini
root = true

[*]
indent_style = space
indent_size = 4
end_of_line = lf
charset = utf-8
trim_trailing_whitespace = true
insert_final_newline = true

[*.{vue,js,ts,json,yaml,yml,scss,css}]
indent_size = 2

[*.md]
trim_trailing_whitespace = false
```

---

### 1.4 启动脚本（Day 5）

#### 1.4.1 后端启动

```bash
cd backend
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

#### 1.4.2 前端启动

```bash
cd frontend
npm run dev
```

#### 1.4.3 根目录一键启动（可选）

创建 `start.ps1`（Windows）和 `start.sh`（Linux/Mac），同时启动前后端。

---

## 二、验收标准

- [ ] `uvicorn app.main:app` 启动成功，`GET /api/health` 返回 `{"status": "ok", "version": "0.1.0"}`
- [ ] `npm run dev` 启动成功，浏览器访问 `http://localhost:5173` 看到 Vite 默认页
- [ ] 前端 `http://localhost:5173/api/health` 通过 Vite proxy 返回后端响应
- [ ] Ruff / ESLint 无错误
- [ ] Git 仓库 `.gitignore` 正确忽略 `node_modules/`、`__pycache__/`、`*.db`
- [ ] 项目可在另一台机器上通过 `git clone` + `npm install` + `pip install` 复现

---

## 三、产出物

| 产出 | 路径 |
|------|------|
| 后端骨架 | `backend/` |
| 前端骨架 | `frontend/` |
| 项目 README | `README.md`（已存在，本阶段更新） |
| 开发规范 | 本文件 `.editorconfig` 节 + 代码注释 |
| 里程碑 tag | `v0.1.0` |
