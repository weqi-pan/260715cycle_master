# Phase 2 最终测试清单

## 前提
- 后端重启（新代码）
- 前端刷新（Ctrl+Shift+R）
- 清除浏览器缓存

---

## A. 状态持久化

- [ ] A1. 开始游戏 → A 节点显示 3 个选项
- [ ] A2. 点击「周边转转」→ 过渡文本出现，关闭 → 新选项出现（观察铜狮、检查台阶、撬铜钱、B1入口、走小巷）
- [ ] A3. 点击「观察铜狮刻字」→ 出现「拓印刻字」选项
- [ ] A4. 点击「拓印刻字」→ 背包出现「铜狮底座拓片」
- [ ] A5. 点击「检查台阶」→ 点击「撬出铜钱」→ 背包出现「清代顺治通宝」+ 跨面标记 ↻
- [ ] A6. 再次周边探索 → 铜钱选项不再出现（条件 `not:has_item:item_qing_coin` 生效）
- [ ] A7. 状态栏 SAN 值变化正确（如减扣或恢复）

## B. 存档系统

- [ ] B1. 点击「💾 存档」→ 输入名称 → 提示「存档成功」
- [ ] B2. 点击「📂 读档」→ 看到存档列表（含节点ID和循环数）
- [ ] B3. 点击「读取」→ 游戏恢复到存档状态
- [ ] B4. 点击「删除」→ 确认后存档消失
- [ ] B5. 存档时 flags 和 inventory 被保留，读档后仍在
- [ ] B6. 无存档时列表显示「暂无存档」

## C. 打字机效果

- [ ] C1. 进入节点后文本逐字显示（约25ms/字）
- [ ] C2. 打字过程中点击文本区域 → 立即显示全文（跳过）
- [ ] C3. 打字过程中选项按钮不出现
- [ ] C4. 打字完毕后选项按钮出现（如有）或 Continue 提示出现（如无）
- [ ] C5. 切换节点时打字机重新开始

## D. 环形小地图

- [ ] D1. 右下角出现环形图标（空心圆 + 弧线）
- [ ] D2. 点击图标展开 SVG 环形地图（160x160）
- [ ] D3. 8 个主节点均匀分布在环上（A-H）
- [ ] D4. 当前所在节点为金色实心圆
- [ ] D5. 已访问节点为半透明圆
- [ ] D6. E 位于 A 正下方（底部），有虚线弧线示意莫比乌斯扭转
- [ ] D7. 获得 `taoist_chant` flag 后，中心出现虚线圆 K 标记
- [ ] D8. 点击图标再次折叠地图

## E. 跨循环持久化

- [ ] E1. 选择一个带 `leave_item` 效果的选项 → 道具留在当前节点
- [ ] E2. 下一轮循环到达同一节点 → 状态栏显示遗留道具
- [ ] E3. A↔E 跨面：在 A 遗留 `cross_surface=true` 道具 → 到达 E 时出现

## F. 环形遍历 + 特殊路由

- [ ] F1. A → B → C → D → E → F → G → H → A 完整可走
- [ ] F2. H → A 时循环计数 +1，顶部状态栏更新
- [ ] F3. K 跃迁：持有 `taoist_chant` → 出现跃迁选项 → 进入 K → 选择目标跳转
- [ ] F4. J 捷径：持有 `know_secret_tunnel` 或地图 → E 出现密道 → J → A（半循环）

## G. 回归检查

- [ ] G1. `backend: python -m pytest tests/ -v` → 24 passed
- [ ] G2. `frontend: npx vue-tsc --noEmit` → 零错误
- [ ] G3. 页面刷新后回到 A 节点重新开始（状态重置）
- [ ] G4. 快速连续点击多个选项不崩溃
- [ ] G5. 所有 JSON 文件无语法错误

---

> **完成标准**: G1-G5 全部通过 + A-F 每项至少 1 条通过
