# Phase 3 测试清单 — 可视化编辑器

## 前提
- 后端重启（新代码，editor API 已更新）
- 前端刷新
- 访问 http://localhost:5173/editor

---

## A. 编辑器 UI 渲染

- [ ] A1. 页面加载后三栏布局正常（左 220px | 中 flex | 右 300px）
- [ ] A2. 左侧节点列表显示所有 30 个节点
- [ ] A3. 节点列表可按类型筛选（主节点/子节点/捷径/跃迁）
- [ ] A4. 中间画布显示环形节点图（Cytoscape.js）
- [ ] A5. 8 个主节点按环形排列，子节点散落在周围
- [ ] A6. 节点间连线（边）可见，箭头指向目标节点
- [ ] A7. 右侧属性面板初始显示「选择一个节点以编辑属性」

## B. 节点操作

- [ ] B1. 点击左侧列表节点 → 中间画布对应节点高亮
- [ ] B2. 点击中间画布节点 → 右侧面板显示节点属性
- [ ] B3. 修改节点名称 → 保存 → 刷新后保留
- [ ] B4. 修改节点正文 → 保存 → 游戏端可见变化
- [ ] B5. 修改节点类型 → 标签颜色变化
- [ ] B6. 点击「+ 新建」→ 输入 ID → 新节点出现在列表和画布
- [ ] B7. 点击节点 × 删除 → 确认 → 节点和连线移除

## C. 边（Choice）操作

- [ ] C1. 选中节点后右侧显示该节点的所有选项
- [ ] C2. 点击「+ 添加选项」→ 新增一个默认选项
- [ ] C3. 修改选项文本、目标节点、条件 → 保存 → 刷新后保留
- [ ] C4. 修改优先级 → 选项顺序变化
- [ ] C5. 勾选/取消「不可用时隐藏」→ 保存生效
- [ ] C6. 点击选项 × 删除 → 选项移除
- [ ] C7. 在画布中拖拽连线（mousedown node → 输入目标ID）→ 创建新边

## D. 数据一致性

- [ ] D1. 编辑器修改节点内容 → 切换到 /play → 游戏内容同步更新
- [ ] D2. 编辑器修改选项 → 切换到 /play → 选项同步更新
- [ ] D3. 编辑器新建节点 → import_story.py 重新导入后不丢失

## E. 回归检查

- [ ] E1. `backend: python -m pytest tests/ -v` → 24 passed
- [ ] E2. `frontend: npx vue-tsc --noEmit` → 零错误
- [ ] E3. /play 页面仍然正常工作
- [ ] E4. /editor 和 /play 可以同时打开，互不干扰
