[pytest]
# Playwright E2E 测试配置
testpaths = tests/e2e

addopts =
    -v
    --tb=short

# 超时设置 (ms)
timeout = 30000

# 标记
markers =
    slow: 慢速测试
    smoke: 冒烟测试
    state: 状态持久化测试
    navigation: 路径遍历测试
    ui: 前端交互测试
